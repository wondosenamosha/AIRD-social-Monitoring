# --- AIRD dashboard runtime dependencies (serving only) ---
# PyTorch (CPU build) is installed separately in ../nixpacks.toml to avoid the
# huge default CUDA wheel. Local dev: `pip install torch` once.
flask==3.1.2
gunicorn==23.0.0
transformers>=4.40,<5
safetensors>=0.4.0
spacy>=3.7,<4
nltk>=3.8
contractions>=0.1.73
numpy<2
praw>=7.7          # Reddit API client for the "analyze a link" feature

# Classical + Custom-Transformer Track B serving (all six models live).
# scikit-learn/scipy/joblib load the TF-IDF+classifier bundles; lightgbm runs
# the LGBM model (needs an OpenMP runtime — libgomp from gcc on Railway,
# `brew install libomp` locally on macOS).
scikit-learn>=1.4,<1.7
scipy>=1.11
joblib>=1.3
lightgbm>=4.0
